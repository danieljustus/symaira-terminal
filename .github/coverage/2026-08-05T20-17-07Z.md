<!-- coverage: timestamp=2026-08-05T20:17:07Z repo=danieljustus/symaira-terminal head=ed3e766c1b6901bde2b2d4bf8844b07cc9d70fa6 overall=61.48 patch=100.00 baseline=33.35 delta=n/a mutation=n/a assertdensity=n/a slowtests=n/a suite=n/a -->

# Coverage Review — 2026-08-05

## Summary
- Tooling: Swift Package Manager with `swift test --enable-code-coverage`, Xcode 27 beta toolchain, and `llvm-cov export -format=lcov`.
- Command: `DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SymairaKit --enable-code-coverage --filter '^(?!.*ControlServer).*' -j 6`.
- Result: pass for the filtered run; the unfiltered local run was blocked by the pre-existing `ControlServerClientTests` integration suite hanging during teardown.
- Overall: lines **61.48%** (1,309/2,129 instrumented non-test lines in the filtered run).
- Patch coverage: **100.00%** of changed production lines; the changed `ClaudeCodeReader.read(since:)` delegation is exercised by the reader tests.
- Baseline: **33.35%** on the prior full 7,821-line instrumented surface. The nominal filtered-run difference is +28.13 percentage points, but the scopes are not comparable, so `delta=n/a` for the formal baseline metric.
- Mutation score: n/a (no mutation tool configured).
- Test quality: assert density, AAA structure, and slow-test extraction were not measured in this scoped run.
- Gate: no repo-configured coverage gate; `threshold=80 (default, no repo gate found)`.

## Metrics
| Area | Lines | Covered | Coverage | Notes |
|---|---:|---:|---:|---|
| Filtered instrumented non-test surface | 2,129 | 1,309 | 61.48% | ControlServer integration suite excluded because it hangs locally |
| **Patch (changed production lines)** | 1 | 1 | 100.00% | `ClaudeCodeReader.read(since:)` delegates to the cache-aware path |

## Test quality
- **Mutation:** n/a — no mutation tool is configured.
- **Assert density:** n/a for this scoped run.
- **AAA:** n/a for this scoped run.
- **Performance:** n/a for this scoped run. The excluded pre-existing integration suite hangs locally during teardown; CI's unfiltered Build & Test job passed for PR #341.

## Findings
No new issue-worthy coverage findings were created. The change adds behavioral assertions for the previously untested ACP client paths and the recently bounded Claude transcript reader, while preserving the existing test suite behavior.

## Non-issues
- The baseline report measured the complete 7,821-line instrumented surface; the filtered run necessarily reports a smaller instrumented set because the local ControlServer integration suite could not complete. The percentage difference must not be interpreted as a formal regression/improvement delta.
- Pure UI layout coverage remains outside this change's scope.
